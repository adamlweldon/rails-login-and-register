<!--the @product and @comment is referring to the def new in Comments controller -->
<%= simple_form_for [@user, @comment] do |f| %>
	<%= f.input :comment %>
	<%= f.button :submit, :class=> "btn btn-primary"%>
<% end %>
